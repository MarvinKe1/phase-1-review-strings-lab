// Write your code in this file!

const currentUser = 'Grace Hopper';

// Define welcomeMessage with 'Welcome to Flatbook, ' and the value of currentUser
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define excitedWelcomeMessage with 'WELCOME TO FLATBOOK, ' and the uppercase value of currentUser
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Define shortGreeting with the first initial of currentUser
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;

